const arrayClicks = [
  {clientX: 528},
  {clientX: 130},
  {clientX: 255},
  {clientX: 308},
  {clientX: 40},
];

const largeX = // Using functional collection APIs you *already* know
               // for arrays, create largeX to be an array of x position
               // numbers from the arrayClicks that are larger than 200.

largeX.forEach(x => console.log(x));
