const array = ['1', '2', '4', null, 'a', '6', '8', '9'];

// Calculate this constant as the sum of all *numbers* (only) in the array.
// The console output should report:
// > 30
const sum = // ...

console.log(sum);
